<%= form_with(model: current_user.active_follows.build, local: false) do |f| %>

<div><%= hidden_field_tag :followed_id, user.id %></div>

<%= f.submit "フォローする", class: "btn btn-outline-primary btn-block" %>
<% end %>